<div class="hero" id="test">
	{{#with hero}}
	<div class="hero-content">{{>hero}}</div>
	<div class="slider">
		<div class="slider-item slider-left">--</div>
		<div class="slider-item slider-right">--</div>
		<img src="{{photo}}" alt="photo" class="hero-img">
	</div>
	{{/with}}
</div>

<main class="blog">
	{{#each blog}}
	{{>template-card}}
	{{/each}}
</main>

<div class="form">
	{{#with form}}
	<div class="form-content">
		<h3 class="form-title">{{title}}</h3>
		<p class="form-text">{{info}}</p>
	</div>
	<a href="{{href}}" class="button form-button">{{button}}</a>
	{{/with}}
</div>

<div class="position-container">
	<div class="position-test">{{hero.text}}</div>
	<a class="position" href="#test"></a>
</div>

<main class="blog">
	{{#each blog}}
	{{>template-card}}
	{{/each}}
</main>

<main class="blog">
	{{#each blog}}
	{{>template-card}}
	{{/each}}
</main>

<main class="blog">
	{{#each blog}}
	{{>template-card}}
	{{/each}}
</main>

<div>
	{{hero.text}} {{hero.text}}
	<img src="{{hero.photo}}" alt="phto" class="img-float">
	<p class="clear-text">{{hero.text}} {{hero.text}} {{hero.text}} {{hero.text}} {{hero.text}}</p>
	<input type="text" class="caret">
</div>